function showFullName(fName,sName){
var fullName = fName + sName;
document.write(  fullName + '<br>' );
}

showFullName('Charlotte', 'Croshaw');
showFullName('Sam' 'Smith');
showFullName('Demi' 'Moore');